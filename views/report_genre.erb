<div>
  <h2>Books by Genre</h2>
  <p class="subheading">Explore your books organized by genre</p>
  
  <% if @books_by_genre.empty? %>
    <p>No books in your catalog yet. <a href="/add">Add your first book</a>.</p>
  <% else %>
    <% @books_by_genre.each do |genre, books| %>
      <div class="report-section">
        <h3 class="report-title"><%= genre %> (<%= books.length %>)</h3>
        <table>
          <thead>
            <tr>
              <th>Title</th>
              <th>Author</th>
              <th>Year</th>
            </tr>
          </thead>
          <tbody>
            <% books.each do |book| %>
              <tr>
                <td><%= book.title %></td>
                <td><%= book.author %></td>
                <td><%= book.year %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% end %>
  <% end %>
</div>
